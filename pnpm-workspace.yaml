packages:
  - examples/*
  - packages/*
  # with-electron-and-walltime is a self-contained example with its own
  # pnpm workspace and a different Node version; keep it out of the root one.
  - "!examples/with-electron-and-walltime/**"
