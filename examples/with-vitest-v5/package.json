{
  "name": "with-vitest-v5",
  "private": true,
  "type": "module",
  "scripts": {
    "bench-vitest": "vitest bench --run"
  },
  "devDependencies": {
    "@codspeed/vitest-plugin": "workspace:*",
    "typescript": "^5.1.3",
    "vitest": "^5.0.0"
  }
}
