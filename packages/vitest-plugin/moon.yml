tasks:
  bench:
    command: vitest bench --run
    inputs:
      - "benches/**"
    local: true
    options:
      cache: false
    deps:
      - build

  test:
    command: vitest --run
    inputs:
      - "./vitest.config.ts"

  test/integ:
    command: noop
