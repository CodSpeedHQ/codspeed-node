tasks:
  clean:
    args:
      - build
  build:
    deps:
      - "build-native-addon"

  build-native-addon:
    command: prebuildify --napi --strip
    inputs:
      - "src/measurement.cc"
      - "binding.gyp"
    outputs:
      - "prebuilds"
