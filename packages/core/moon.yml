tasks:
  clean:
    args:
      - build
  build:
    deps:
      - "build-native-addon"

  build-native-addon:
    command: "node-gyp rebuild"
    platform: "system"
    inputs:
      - "src/measurement.cc"
      - "binding.gyp"
    outputs:
      - "build/"
