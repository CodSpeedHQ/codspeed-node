tasks:
  bench:
    command: tsx benches/sample.ts
    inputs:
      - "benches/**"
    local: true
    platform: "system"
    options:
      cache: false
    deps:
      - build

  test:
    command: vitest --run
    inputs:
      - "./vitest.config.ts"

  test/integ:
    command: noop
