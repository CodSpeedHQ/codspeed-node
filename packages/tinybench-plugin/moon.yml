tasks:
  bench:
    command: node -r esbuild-register benches/sample.ts
    inputs:
      - "benches/**"
    local: true
    platform: "system"
    options:
      cache: false
  test:
    command: vitest --run
    inputs:
      - "./vitest.config.ts"

  test/integ:
    command: noop
